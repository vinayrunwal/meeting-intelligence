[tool.vercel]
entrypoint = "dev_server:app"

[project]
name = "meeting-intelligence"
version = "0.1.0"
description = "Real-Time Multilingual Meeting Intelligence System"
requires-python = ">=3.10"

dependencies = [
  "-r requirements.txt"
]

